"""
Домашнее задание 1 (Python Advanced · Flask)

Задание 1:
- Найти ошибку в коде: маршрут должен начинаться со "/" а не с пустой строки "".

Задание 2:
- Создать простое Flask-приложение:
  1) Создать файл main.py
  2) Импортировать Flask и создать объект приложения
  3) Добавить маршрут на корневой URL ("/"), который возвращает текст "Привет, Flask!"
  4) Добавить маршрут /user/<name>, который возвращает приветствие с именем
  5) Запустить и протестировать приложение
"""

from flask import Flask

# Создаём объект Flask-приложения
app = Flask(__name__)

# Маршрут для корневого URL (http://127.0.0.1:5000/)
@app.route("/")
def home():
    # Возвращаем простой текст
    return "Привет, Flask!"

# Дополнительный маршрут с параметром <name>
# Пример: http://127.0.0.1:5000/user/Женя
@app.route("/user/<name>")
def greet(name: str):
    # Возвращаем приветствие с подставленным именем
    return f"Привет, {name}!"

# Точка входа: запуск приложения
if __name__ == "__main__":
    # debug=True включает автоматическую перезагрузку и детальные ошибки
    app.run(debug=True)
